object Modules {

    const val app = ":app"
    const val components = ":components"
    const val users = ":users"
    const val datasource = ":datasource"

    val modules = listOf(
        app,
        components,
        users,
        datasource,
    )
}